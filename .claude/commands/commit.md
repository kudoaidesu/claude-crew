# Git Commit

変更内容を確認し、適切なコミットメッセージでコミットします。

## 手順
1. `git status` で変更を確認
2. `git diff` で差分を確認
3. 変更内容を要約したコミットメッセージを作成
4. `git add` と `git commit` を実行

## コミットメッセージ形式
```
<type>: <日本語で簡潔な説明>

<詳細説明（必要に応じて）>
```

type: feat, fix, refactor, docs, test, chore
